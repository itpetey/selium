package operations.launch.v1;

schema LaunchRecord {
  service: string;
  retries: i32;
  mode: string;
}

event launch.recorded(LaunchRecord) {
  partitions: 2;
  retention: "1h";
  delivery: at_least_once;
  replay: enabled;
}
