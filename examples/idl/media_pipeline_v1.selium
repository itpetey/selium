package media.pipeline.v1;

schema Frame {
  camera_id: string;
  ts_ms: u64;
  jpeg: bytes;
}

event camera.frames(Frame) {
  partitions: 12;
  retention: "24h";
  delivery: at_least_once;
  replay: enabled;
}
