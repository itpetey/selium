# Rust SDK Examples

Standalone Rust examples showing the new SDK surface.

Run typed pub/sub:

```bash
cargo run --manifest-path examples/rust-sdk/Cargo.toml --bin typed_pubsub
```

Run byte-level pub/sub:

```bash
cargo run --manifest-path examples/rust-sdk/Cargo.toml --bin byte_pubsub
```
