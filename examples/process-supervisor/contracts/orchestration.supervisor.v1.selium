package orchestration.supervisor.v1;

schema WorkerStatus {
  worker_id: u32;
  phase: string;
}

event supervisor.worker_status(WorkerStatus) {
  partitions: 4;
  retention: "1h";
  delivery: at_least_once;
  replay: enabled;
}
