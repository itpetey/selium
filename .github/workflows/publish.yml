name: Publish Artifacts

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Placeholder
        run: |
          echo "Publishing is intentionally disabled during monorepo bootstrap."
