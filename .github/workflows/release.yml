name: Draft Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  draft:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Placeholder
        run: |
          echo "Release automation will be wired after control-plane + SDK stabilization."
