[package]
name = "selium-guest"
version.workspace = true
edition.workspace = true
description = "Selium guest SDK"
readme.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
axum = { workspace = true, features = ["http1", "tokio"], optional = true }
bytes.workspace = true
flatbuffers.workspace = true
futures = { workspace = true, features = ["std"] }
quinn = { workspace = true, optional = true }
selium-abi.workspace = true
selium-guest-macros = { path = "macros" }
thiserror.workspace = true
tokio = { workspace = true, features = ["io-util"], optional = true }
tokio-util = { workspace = true, features = ["codec"] }
tracing.workspace = true
tracing-subscriber = { workspace = true, features = ["registry"], optional = true }

[build-dependencies]
flatbuffers-build.workspace = true
flatc-fork.workspace = true

[dev-dependencies]
flatbuffers.workspace = true
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "time"] }

[features]
default = ["io", "logging"]
axum = ["dep:axum", "io", "tokio/time"]
io = ["dep:tokio"]
logging = ["dep:tracing-subscriber"]
quinn = ["dep:quinn", "io"]

[lints]
workspace = true
